<% if user_signed_in? %>
  <%= link_to('Edit Profile', edit_user_registration_path , :class => "btn btn-inverse pull-right") %>
<% else %>
  <%= link_to('Register', new_user_registration_path , :class => "btn btn-inverse pull-right")  %>
<% end %>