en:
  errors: &errors
    messages:
      at_least_one_entry: at least one option has to be selected
      minimum_entries: at least %{count} options need to be selected
      maximum_entries: not more than %{count} options can be selected
      minimum_categories: at least one option has to be selected
      maximum_categories: not more than 2 options can be selected
  activemodel:
    errors:
      <<: *errors
  activerecord:
    errors:
      <<: *errors
