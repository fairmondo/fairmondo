javascript:
  $('#controller_css').replaceWith("#{j stylesheet_link_tag asset_path(controller_specific_css_path), id: 'controller_css' }");
  $('#l-footer-nav-left .socialshareprivacy').remove() // only needed on welcome page which doesnt have data-push links to it
  $('#l-footer-nav-left .sprite_gemeinwohloekonomie').parent().remove()
= content_for :view_specific_scripts
- wiselinks_title ( @page_title || t('common.fairmondo') )
= yield
