#auction_form_container
  = tinymce_assets
  = render :partial => "auction_template_select"
  = semantic_form_for @auction, :html => {:class => "form-vertical", :multipart => true}, :builder => Fairtastic::FormBuilder do |f|
    = f.semantic_errors
    = render :partial => "common_inputs", :locals => {:f => f}
    = f.input_step :double_check, :prefix => false do
      = render :partial => "auction_template_create_inputs"
      = f.actions do
        = f.action :submit , :label => t("auction.labels.continue_to_preview")
