<% if user_signed_in? %>
  <%= link_to(t('common.actions.edit_profile'), edit_user_registration_path , :class => "btn pull-right") %>
<% else %>
  <%= link_to(t('common.actions.register'), new_user_registration_path , :class => "btn pull-right")  %>
<% end %>