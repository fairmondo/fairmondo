commentable_selector = '<%= "##{@commentable.class.to_s.underscore}#{@commentable.id}" %> ';

$(commentable_selector + '.Comments-section .Comment-single').remove();

$(commentable_selector + '.Comments-section .Comments').html($("<%= j render @comments %>"));

$(commentable_selector + '.Comments-section .next_link').remove();
$(commentable_selector + '.Comments-section .all_link').remove();

