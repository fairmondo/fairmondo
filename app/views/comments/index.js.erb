$('<%= "##{commentable.class.to_s.parameterize}#{@commentable.id}" %> .Comment-single').last().after('<%= render "comment", collection: @comments %>');

$('<%= "##{commentable.class.to_s.parameterize}#{@commentable.id}" %> .next_link').replaceWith("<%= j(
    link_to_next_page @comments,
    t('common.text.morecomments'),
    param_name: :comments_page,
    params: { library_id: @commentable.id, controller: "comments" },
    class: "next_link",
    remote: true) %>"
);
