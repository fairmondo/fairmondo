- if @articles.empty?
  .fairnopoly-well
    %h3= t("common.text.no_search_result")
    = t("common.text.no_search_result_text")
- else
  = render :partial=>'/shared/list_articles', :locals=>{ :articles=> @articles, :list_class=>"span3"}
  = will_paginate @articles
