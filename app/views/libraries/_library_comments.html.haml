.Comments-section
  - if current_user
    = render "comments/new", library: library
  - else
    = t('common.text.login_to_comment')
  = render(library.comments.order(created_at: :desc)) || t('common.text.no_comments')
